# Ignore plugin installations and runtime data managed by Neovim
.netrwhist
undo/
swp/

# Optional: If you use a plugin manager like Lazy.nvim, do NOT ignore lazy-lock.json.
# It tracks your exact plugin versions, acting like a package-lock.json. Keep it tracked!
